# This is a Databricks asset bundle definition for dab_project_databricks.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project_databricks
  uuid: ebe8a777-71f7-439e-9376-19aa356e8081


include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}_
      source_linked_deployment: false 
      
    workspace:
      host: https://dbc-1045367d-1248.cloud.databricks.com/
      root_path: /Workspace/Users/sairam1582003@gmail.com/DAB/DEV/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      host: https://dbc-1045367d-1248.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/sairam1582003@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sairam1582003@gmail.com/DAB/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
